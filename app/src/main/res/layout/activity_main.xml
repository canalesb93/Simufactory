<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView android:text="Simufactory" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/title"
        android:textAlignment="center"
        android:textSize="20sp"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/sessionName" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/sessionListView"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/newSessionButton" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/sessionPassword"
        android:hint="Password"
        android:layout_below="@+id/sessionName"
        android:layout_alignEnd="@+id/sessionName"
        android:layout_alignStart="@+id/sessionName" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/sessionName"
        android:hint="Name"
        android:layout_below="@id/title"
        android:layout_alignEnd="@+id/newSessionButton"
        android:layout_alignStart="@+id/newSessionButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Crear Sesion"
        android:id="@+id/newSessionButton"
        android:layout_below="@+id/sessionPassword"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
